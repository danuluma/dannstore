language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install coveralls codecov

script:
  - python3 -m unittest
  - coverage run --source=app -m unittest
  - coverage report

after_success:
  - coveralls
  - codecov
